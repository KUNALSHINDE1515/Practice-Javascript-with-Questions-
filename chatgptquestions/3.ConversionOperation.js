// console.log("naskar duniya");
//=================================================================Conversion OPearation In js =========================================================================
// String to Number Conversion:
// Write a function that takes a string as input and converts it to a number. Ensure the function handles different cases such as integers, floating-point numbers, and invalid inputs gracefully.


// Number to String Conversion:
// Write a function that takes a number as input and converts it to a string. Make sure to handle edge cases such as NaN and Infinity appropriately.

// Array to String Conversion:
// Create a function that takes an array of elements and converts it into a string, separating each element with a comma. For example, convertToString([1, 2, 3]) should return "1,2,3".

// String to Array Conversion:
// Write a function that takes a string of comma-separated values and converts it into an array. For example, convertToArray("1,2,3") should return [1, 2, 3].

// Boolean to String Conversion:
// Create a function that takes a boolean value and converts it into a string representation ("true" or "false").

// String to Boolean Conversion:
// Write a function that takes a string and converts it into a boolean value. For example, "true" should be converted to true and "false" to false.

// Number to Boolean Conversion:
// Create a function that takes a number and converts it into a boolean value. Consider what values should be considered true and which should be considered false.

// Boolean to Number Conversion:
// Write a function that takes a boolean value and converts it into a number (0 for false, 1 for true).

// Date to String Conversion:
// Create a function that takes a JavaScript Date object and converts it into a string in a specified format (e.g., "YYYY-MM-DD").

// String to Date Conversion:
// Write a function that takes a string representing a date and converts it into a JavaScript Date object.